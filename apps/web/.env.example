# Local development only; hosted values are configured through Sites.
# Independent 32-byte random token, hex encoded. Never a KeeperHub wallet key.
RETURN_MONITOR_TOKEN=
# Enables authenticated read-only samples, not a scheduler or transactions.
RETURN_MONITOR_ENABLED=false
